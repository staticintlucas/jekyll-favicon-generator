# frozen_string_literal: true

RSpec.describe JekyllFaviconGenerator do
  it "has a version number" do
    expect(JekyllFaviconGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).not_to eq(true)
  end
end
